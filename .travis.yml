language: cpp

compiler:
  - gcc

notifications:
  email: false

before_install:
  - ./scripts/update_cmake.sh
  - wget https://github.com/gflags/gflags/archive/16a1687.zip
  - unzip 16a1687.zip
  - cd 16a1687
  - mkdir build && cd build
  - cmake ..
  - sudo make install
  - cd ../..

script:
  - mkdir build && cd build
  - cmake ..
  - make
